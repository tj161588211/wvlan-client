<UserControl x:Class="WVLAN.Client.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:WVLAN.Client.Converters"
             mc:Ignorable="d">
    
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 连接控制区 -->
        <Border Grid.Row="0" 
                Background="{DynamicResource CardBackground}"
                CornerRadius="12"
                Padding="24"
                Margin="0,0,0,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- 状态信息 -->
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Connection Status" 
                               FontSize="14" 
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource AccentColor}"/>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                        <Ellipse Width="16" Height="16" 
                                 Fill="{Binding ConnectionStatusColor}"
                                 RadiusX="8" RadiusY="8"
                                 Margin="0,0,12,0"/>
                        <TextBlock Text="{Binding ConnectionStatusText}" 
                                   FontSize="24"
                                   FontWeight="Bold"/>
                    </StackPanel>

                    <TextBlock Text="{Binding LocalAddress}" 
                               FontSize="14"
                               Opacity="0.7"
                               Margin="0,8,0,0"/>
                </StackPanel>

                <!-- 控制按钮 -->
                <StackPanel Grid.Column="1" Orientation="Vertical" HorizontalAlignment="Right">
                    <Button Content="Connect"
                            Command="{Binding DataContext.ConnectCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                            IsEnabled="{Binding DataContext.CanExecuteConnect, RelativeSource={RelativeSource Self}}"
                            Style="{StaticResource PrimaryButtonStyle}"
                            MinWidth="120"
                            Margin="0,0,0,10"/>
                    
                    <Button Content="Disconnect"
                            Command="{Binding DataContext.DisconnectCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                            IsEnabled="{Binding DataContext.CanExecuteDisconnect, RelativeSource={RelativeSource Self}}"
                            Style="{StaticResource SecondaryButtonStyle}"
                            MinWidth="120"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 统计卡片 -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding DashboardCards}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="3"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource CardBackground}"
                                CornerRadius="12"
                                Padding="20"
                                Margin="0,0,20,20">
                            <StackPanel>
                                <TextBlock Text="{Binding Title}" 
                                           FontSize="14"
                                           Opacity="0.7"/>
                                <TextBlock Text="{Binding Value}" 
                                           FontSize="32"
                                           FontWeight="Bold"
                                           Foreground="{DynamicResource AccentColor}"
                                           Margin="0,8,0,0"/>
                                <TextBlock Text="{Binding Unit}" 
                                           FontSize="12"
                                           Opacity="0.5"
                                           Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
